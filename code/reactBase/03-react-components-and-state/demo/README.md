# Demo：待办事项状态项目

本目录已经放置本章可运行 Demo。

已实现：

- `TodoApp`
- `TodoForm`
- `TodoList`
- `TodoItem`
- 筛选控件

运行方式：

- 直接用浏览器打开 `index.html`。
- 需要能访问 React CDN。

核心交互：

- 父传子展示 todo。
- 子传父切换完成状态。
- 受控输入新增任务。
- 全部/未完成/已完成筛选。
