# Demo：搜索列表和 Hooks

本目录已经放置本章可运行 Demo。

已实现：

- Tab 切换。
- 模拟数据加载。
- 搜索输入。
- `useToggle`。
- 可选 `useDebounce`。

运行方式：

- 直接用浏览器打开 `index.html`。
- 需要能访问 React CDN。

核心交互：

- Tab 切换。
- 搜索触发模拟加载。
- `useEffect` 清理计时器。
- `useToggle` 控制弹窗。
